class SuggestionsController < ApplicationController
  def index
    # datetime = 1.year.ago
    # youtube_account = YoutubeAccount.find(params["youtube_account_id"])
    # Suggestion.create_suggestions(youtube_account, datetime)
    # @channel_suggestions = Suggestion.where(watcher: youtube_account, type: "Channel")
    # @video_suggestions = Suggestion.where(watcher: youtube_account, type: "Video")
  end

  def show
    # @suggestion = Suggestion.find(params["id"])
  end
end
